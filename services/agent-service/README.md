# Agent Service Boundary

The MVP keeps report synthesis and QA inside the API service for faster delivery.
This directory is reserved for extraction into a dedicated LangGraph-based agent worker later.